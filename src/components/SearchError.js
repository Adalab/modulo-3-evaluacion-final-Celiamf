import "../stylesheets/_SearchError.scss";

const SearchError = () => {
  return (
    <p className="searchErrorText">
      Sorry, your search did not match any character in our list.
    </p>
  );
};

export default SearchError;
